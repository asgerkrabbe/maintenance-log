version: '3.8'
services:
  app:
    build: .
    environment:
      - APP_PORT=8080
      - DB_FILE_PATH=/data/moto-maintenance
      - ADMIN_USER=admin
      - ADMIN_PASSWORD=changeme
    ports:
      - "8080:8080"
    volumes:
      - moto-data:/data
volumes:
  moto-data:
